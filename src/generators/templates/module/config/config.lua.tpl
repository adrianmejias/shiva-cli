--- {{module_name}} — Configuration
--- Modify these values to configure the module behaviour.

Config = Config or {}

Config.{{PascalName}} = {
    enabled = true,
    debug   = false,

    -- TODO: Add module configuration options
}
