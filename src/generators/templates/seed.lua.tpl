return {
    run = function(db)
        -- TODO: Insert seed data
        -- Example:
        -- db.execute([[
        --     INSERT IGNORE INTO `{{table_name}}` (`id`, `name`) VALUES
        --     (1, 'example')
        -- ]])
    end,
}
